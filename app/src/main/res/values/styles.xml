<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" >
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/second_color</item>
        <item name="colorPrimaryDark">@color/second_color</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/first_orange_color</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MyLoginEditext">
        <item name="android:colorControlNormal">#d2c5bd</item>
        <item name="android:colorControlActivated">#ffffff</item>
        <item name="android:colorControlHighlight">#eb0064a7</item>
    </style>

    <style name="myTheme" parent="AppTheme" />

</resources>
